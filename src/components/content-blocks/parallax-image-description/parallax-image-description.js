import React from 'react';
import styled from "@emotion/styled";

const StyledParallaxImageDescription = styled.div`
  height: 300px;
  width: 100%;
  transform-style: preserve-3d;
`;

const ParallaxImageDescription = () => {
  return (
    
  )
};

export default ParallaxImageDescription;
